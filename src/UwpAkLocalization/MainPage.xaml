<Page
    x:Class="UwpAkLocalization.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpAkLocalization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:extensions="using:UwpAkLocalization.Extensions"
    xmlns:converters="using:UwpAkLocalization.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Mode=OneWay, RelativeSource={RelativeSource Self}}">
<Page.Resources>
    <converters:LocalizationConverter x:Key="LocalizationConverter"/>
</Page.Resources>
    <StackPanel Orientation="Vertical">
        <!--<TextBlock Text="{x:Bind Path=local:LocalizationKeys.HomePage_HelloWorld, Converter={StaticResource LocalizationConverter}}"></TextBlock>-->
        <TextBlock Text="{Binding Localization[HomePage:HelloWorld] }"></TextBlock>
        <!--<TextBlock Text="{Binding Localization[HomePage:Welcome\, new \{username:Bob\}] }"></TextBlock>-->
        <Button Click="ButtonBase_OnClick">French</Button>
    </StackPanel>
</Page>
